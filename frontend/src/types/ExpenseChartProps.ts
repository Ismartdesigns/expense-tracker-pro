export interface ExpenseChartProps {
  dateRange: {
    start: string; // or Date, depending on your implementation
    end: string;   // or Date, depending on your implementation
  };
  // Add other props as needed
}
